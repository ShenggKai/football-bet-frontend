// project import
import AppMenu from './AppMenu';

// ========================|| App Sidebar ||========================
const AppSidebar = () => {
    return <AppMenu />;
};

export default AppSidebar;
